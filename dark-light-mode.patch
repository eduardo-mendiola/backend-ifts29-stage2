--- a/views/layout.pug
+++ b/views/layout.pug
@@ -6,6 +6,8 @@
     meta(name="viewport", content="width=device-width, initial-scale=1")
     link(rel="stylesheet", href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css")
     link(rel="stylesheet", href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.css")
+    link(rel="stylesheet", href="/css/themes.css")
     link(rel="icon" type="image/x-icon" href="/images/favicon.webp")
+    script(src="/js/theme-toggle.js")
 
     style.
@@ -16,8 +18,8 @@
       }
 
       body {
-        background-color: #121212;
-        color: #f0f0f0;
+        background-color: var(--bg-primary);
+        color: var(--text-primary);
       }
 
       .main-container {
@@ -30,8 +32,8 @@
         height: 100vh;
         display: flex;
         flex-direction: column;
-        background-color: #212529;
-        color: white;
+        background-color: var(--bg-sidebar);
+        color: var(--text-sidebar);
         position: sticky;
         top: 0;
       }
@@ -47,11 +49,11 @@
         width: 6px;
       }
       .sidebar-content::-webkit-scrollbar-thumb {
-        background-color: #444;
+        background-color: var(--scrollbar-thumb);
         border-radius: 3px;
       }
       .sidebar-content::-webkit-scrollbar-thumb:hover {
-        background-color: #666;
+        background-color: var(--scrollbar-thumb-hover);
       }
 
       .sidebar-footer {
         flex-shrink: 0;
-        border-top: 1px solid rgba(255, 255, 255, 0.1);
+        border-top: 1px solid var(--border-color);
       }
 
@@ -93,7 +95,7 @@
       }
 
       .nav-link {
-        color: #ffffffb3;
+        color: var(--text-sidebar-muted);
         transition: background-color 0.2s, color 0.2s;
         white-space: nowrap;
         text-overflow: ellipsis;
@@ -101,12 +103,12 @@
       }
 
       .nav-link:hover {
-        background-color: #60606033;
-        color: white;
+        background-color: var(--nav-hover);
+        color: var(--text-sidebar);
       }
 
       .nav-link:active {
-        background-color: #60606033;
+        background-color: var(--nav-hover);
         color: #cececeff !important;
       }
 
       .nav-link.active {
-        background-color: #88888833 !important;
-        color: #fff !important;
+        background-color: var(--nav-active) !important;
+        color: var(--text-sidebar) !important;
       }
       
       main {
         flex-grow: 1;
         overflow-y: auto;
-        background-color: #121212;
+        background-color: var(--bg-primary);
         padding: 1.5rem;
       }
 
       table {
-        background-color: #1e1e1e;
+        background-color: var(--table-bg);
       }
 
       .table {
         width: 100%;
-        background-color: #1e1e1e;
+        background-color: var(--table-bg);
         table-layout: auto;
         word-wrap: break-word;
       }
@@ -343,6 +345,10 @@
               strong #{user.employee && user.employee.first_name ? user.employee.first_name + ' ' + user.employee.last_name : user.username}
             ul.dropdown-menu.dropdown-menu-dark.text-small.shadow(aria-labelledby="dropdownUser1")
               li
+                a.dropdown-item(href="#" onclick="toggleTheme(); return false;")
+                  i#theme-icon.bi.bi-moon-fill.me-2
+                  span#theme-text Modo Oscuro
+              li
                 a.dropdown-item(href="#") Configuración
               li
                 a.dropdown-item(href="/profile") Mi perfil
